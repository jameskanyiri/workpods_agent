# Label Gotchas

- Do not create labels that just restate task status or priority.
- Avoid near-duplicate labels such as `urgent`, `high-priority`, and `critical` if priority already exists.
- Scope mistakes become retrieval mistakes later; check `entity_scopes`.
- Read current labels before creating a new one.
