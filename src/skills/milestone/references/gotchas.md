# Milestone Gotchas

- A milestone is not a broad task with a nicer name.
- Do not invent status ids; fetch milestone statuses first.
- If you need to know milestone progress, read project tasks and filter by `milestone_id`.
- Do not leave a milestone with no task path unless the user is explicitly only drafting structure.
