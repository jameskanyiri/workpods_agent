COMPLETE_ONBOARDING_TOOL_DESCRIPTION = """\
Mark the user's onboarding as complete. Call this ONLY after the backend \
registration script (onboarding/scripts/register_user.py) has returned \
successfully via execute_script.

This tool flips the onboarding state so the user gains access to the full \
ADA Finance capabilities. It takes no parameters.
"""
